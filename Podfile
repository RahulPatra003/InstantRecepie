# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'InstantRecepie' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for InstantRecepie
  pod 'Alamofire'
  pod 'SDWebImage'
  pod 'IQKeyboardManagerSwift'
  pod 'IBAnimatable'
  
  target 'InstantRecepieTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'InstantRecepieUITests' do
    # Pods for testing
  end
  
end
